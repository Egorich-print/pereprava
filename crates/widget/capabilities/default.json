{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Pereprava status widget (tray + dashboard).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
