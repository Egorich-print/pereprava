//! Device actor (phase 2a, filled in after mtp-rs API study).

use crate::error::Result;

/// Returns a short banner describing the current scaffold state.
pub fn scaffold_banner() -> String {
    String::from("pereprava-core actor: scaffold")
}

/// Placeholder to keep the module compiling until phase 2a lands.
pub fn ok() -> Result<()> {
    Ok(())
}
